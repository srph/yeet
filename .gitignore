*.log
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
/.codex
/.cursor/
/.idea
/.nova
/.phpunit.cache
/.vscode
/.zed
/auth.json
/node_modules
/public/build
/public/fonts-manifest.dev.json
/public/hot
/public/storage
/storage/*.key
/storage/pail
/vendor
_ide_helper.php
Homestead.json
Homestead.yaml
Thumbs.db

# The parked Next.js app. Its source is committed as a porting reference, but
# its build artifacts are not — and the root /node_modules rule above is
# anchored, so these need naming explicitly.
/legacy/node_modules
/legacy/.next
/legacy/.trigger
/legacy/.env.local

# yt-dlp scratch space: videos land here before upload, then get unlinked.
/storage/app/tmp

# Personal notes
.kier